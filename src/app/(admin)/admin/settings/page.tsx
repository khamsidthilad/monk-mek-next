import { ModulePlaceholder } from "@/components/shared/ModulePlaceholder";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Site-wide config keys (branding, contact, and feature toggles)."
    />
  );
}
