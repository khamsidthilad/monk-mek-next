export {
  LOCALES,
  LOCALES as SUPPORTED_LOCALES,
  LOCALE_COOKIE,
  DEFAULT_LOCALE,
  getMessage,
  isLocale,
  resolveLocale,
  messages,
} from "@/messages/catalog";

export type { Locale, MessagesByLocale } from "@/messages/catalog";
