/** Cookie name — must match edge guard in `src/proxy.ts`. */
export const ADMIN_SESSION_COOKIE = "admin_session";

/** Public admin routes (no session). */
export const ADMIN_LOGIN_PATH = "/admin/login";
