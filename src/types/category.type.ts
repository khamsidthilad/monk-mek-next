export interface CategoryType {
    success: boolean
    data: Category[]
  }
  
  export interface Category {
    cate_id: number
    cate_name: string
    createdAt: string
    updatedAt: string
  }
  